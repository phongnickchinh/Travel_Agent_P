# ========================================
# Docker Compose - Production Stack
# Travel Agent P
# ========================================
# Services: PostgreSQL, Redis, Elasticsearch, Flask, Celery, Nginx
# MongoDB: Uses MongoDB Atlas (cloud)
# Usage: docker-compose -f docker-compose.production.yml up -d --build
# ========================================
services:
  # ============= PostgreSQL Database =============
  postgres:
    image: postgres:12-alpine
    container_name: travel-agent-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DBNAME:-travel_agent_db}
      POSTGRES_USER: ${POSTGRES_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - travel-agent-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USERNAME:-postgres} -d ${POSTGRES_DBNAME:-travel_agent_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============= Redis Cache & Queue =============
  redis:
    image: redis:7-alpine
    container_name: travel-agent-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    networks:
      - travel-agent-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru

  # ============= Elasticsearch (Optional) =============
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:9.2.2
    container_name: travel-agent-elasticsearch
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - cluster.name=travel-agent-cluster
      - node.name=travel-agent-node-1
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    networks:
      - travel-agent-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
    profiles:
      - with-elasticsearch

  # ============= Flask API Application =============
  flask-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: travel-agent-flask
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Database connections (use service names from Docker network)
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      # MongoDB Atlas - from .env file
      # MONGODB_URI will be loaded from .env
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      ELASTICSEARCH_URL: http://elasticsearch:9200
      # Flask settings
      FLASK_ENV: production
      DEBUG: "False"
    ports:
      - "5000:5000"
    volumes:
      - ./logs:/app/logs
      - ./secrets:/app/secrets:ro
    networks:
      - travel-agent-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    command: gunicorn --bind 0.0.0.0:5000 --workers 2 --threads 4 --timeout 120 --access-logfile - --error-logfile - "app:create_app()"

  # ============= Celery Worker =============
  celery-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: travel-agent-celery-worker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      # MongoDB Atlas - from .env file
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      ELASTICSEARCH_URL: http://elasticsearch:9200
    healthcheck:
      disable: true
    volumes:
      - ./logs:/app/logs
      - ./secrets:/app/secrets:ro
    networks:
      - travel-agent-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      flask-app:
        condition: service_healthy
    command: celery -A celery_worker.celery worker --loglevel=info --pool=solo

  # ============= Celery Beat Scheduler =============
  celery-beat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: travel-agent-celery-beat
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      # MongoDB Atlas - from .env file
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
    volumes:
      - ./logs:/app/logs
      - ./secrets:/app/secrets:ro
    networks:
      - travel-agent-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      flask-app:
        condition: service_healthy
    command: celery -A celery_worker.celery beat --loglevel=info

  # ============= Nginx Reverse Proxy =============
  nginx:
    image: nginx:alpine
    container_name: travel-agent-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - ./nginx/logs:/var/log/nginx
    networks:
      - travel-agent-network
    depends_on:
      - flask-app
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3

# ============= Networks =============
networks:
  travel-agent-network:
    driver: bridge

# ============= Volumes =============
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  elasticsearch_data:
    driver: local
